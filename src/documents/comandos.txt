#Crear servidor con python
    python3 -m http.server 9999
#verificar ip 
    ipconfig
#entrar super usuario
    sudo -i
    sudo -s
#Salir de super usuario
    exit
#Instalar herramientas de red
    sudo apt install net-tools
#Mostrar interfaces de red
    ifconfig 
#Mostrar puerta de enlace
    ip r
#Saber cuál es la IP del servidor DNS en Ubuntu
    cat /etc/resolv.conf
#instalar para crear entorno virtual (las instalaciones seran solo para el proyect)
    apt-get install virtualenv  
    apt install python3.10-venv
#activar el entorno
    Scripts/activate
    . myVirtuaEntorno/bin/activate
    source .myVirtuaEntorno/bin/activate
#ver que contiene el ambiente virtual 

#instalar docker 
    https://www.digitalocean.com/community/tutorials/how-to-install-and-use-docker-on-ubuntu-20-04-es
#verificar estatus demonio de docker
    sudo systemctl status docker
#agregar usus a grupo de docker
    sudo groupadd docker
    sudo usermod -aG docker ${USER}    

#verificar docker
id -nG


#habilitar
 sudo systemctl enable docker.service
 sudo systemctl enable containerd.service
#desabilitar
 sudo systemctl disable docker.service
 sudo systemctl disable containerd.service


#Agregar usuario a docker
sudo groupadd docker
 sudo usermod -aG docker $USER
 sudo usermod -aG docker dagata
 newgrp docker
 #verificar
 docker run hello-world

#crear imagen docker de pythom
    docker build -t simpleServer .



comprimir 
rar a nombre_fichero_comprimido.rar nombre_fichero_a_comprimir
rar a nombre_fichero_comprimido.rar *
